
import { HeaderContainer, HeaderIcon, HeaderText } from './Header.styled';

export const Header = () => {
    return (
      <HeaderContainer>
        <HeaderIcon />
        <HeaderText>Tweet-follower</HeaderText>
      </HeaderContainer>
    );
};
